На оригинальном мартиалматч файты на ЮАЙ обновляются очень быстро, а не как у нас каждые 30сек.
Также там доступно текущее время схватки (отсчет назад <span>00:55</span>) и очки:штрафы (<span class="tag mr-2 has-text-light-bold">6:0</span>) каждого участника в этой текущей схватке (00:55 на примере в хтмл).
 данные примера я привел из полного хтмл конкретного файта
server\dev-test-martialmatch-v1\data\websocket\walki.html

И я проверил, что дополнительных запросов на файтс.хтмл браузер не отправляет.
после небольшого рисерча я выяснил, что Вместо этого вкладка файтс на оригинальном мартиалматч устанавливает вебсокет соединение с сервером и подписывается на события по конкретным матам (список нам уже доступен для конкретного ивента).
После того как браузер подписался -- сервер начинает отправлять каждую секунду обновленную информацию по тому что происходит на том мате на который подписался браузер.
За счет этого и отображается таймер обратного отсчета или он останавливается вовремя, если почему-то судья поставил бой на паузу, отображаются смена очков:штрафов каждого участника в текущей схватке и статус схватки (стиль бекграунда карточки схватки, коричневый, зеленый, ...): 
"fightStatus":awaiting/ongoing

Также я заметил, что каждую минуту вебсокет конекшн закрывается (в фаерфоксе на вкладке респонзов вебсокета вижу Connection Closed: 1006 ) и браузер инициирует новый вебсокет коннекшн, снова подписывается на получение изменений по матам и все повторяется. 

в этом я не уверен, т.е. я не уверен, что вебсокет сервер присылает "fightStatus":scheduled
Мы точно видим по примерам что присылаются  awaiting/ongoing, а как есть ли scheduled -- это я поисследую позже, когда будет хоть один живой ивент с реально раюотающим вебсокет сервером.

Пока давай отталкиваться, что статуса по вебсокетам пока 2: "fightStatus":awaiting/ongoing.
А вот актуальную картину по скедюлед файтам (могут появляться динамически например как только определятся участники финала по результатам полуфиналов). Поэтому пока оставим запрос на файтс.хтмл чтобы обновлять раз в 30сек список скедюлед файтов. а делее уже на нем будем менять время схватки, очки или статус по ответам от вебсокет сервера (так часто как сервер будет присылать ответы, вплот до обновлений каждую секунду для времени схватки)


Я хочу в своем приложении тоже законнектится на их вебсокет сервер и получать быстро события от сервера чтобы обновлять время схватки, очки и штрафы и менять стиль карточки при смене fightStatus. (естествеено придется делать свой прокси сервер чтобы обойти серверную защиту origin, но об этом поговорим позже). Пока давай о самой идеею Все ли ясно? Что думаешь?


я немного насобирал артефактов работы вебсокетов здесь server\dev-test-martialmatch-v1\data\websocket
Посмотри все ли ясно?

отлично, мне все наравится из того что ты предложил.
Список файтов мы подгружаем запросом файтс.джейсон при каждом переключении на табу файтс + посылаем этот запрос заново либо каждые 30 сек находясь на табе файтс без переключений либо по принудительному клику на кнопку табы файтс. На кнопка мы уже показываем обратный отсчет до следующего обновления списка файтов, т.е. до следующего запланированного fights.json.

Можешь ли уже набросать план того что и как мы будем имплементить по шагам. 
Мы продолжим с тобой обсуждать и менять-расширять этот план до начала имплементации, но мне кажется уже сейчас ясна идея с чего начать и в чем наша конечная цель. Есть ли смысл переключитьтся в план моде?

Также сразу поднакину идей:
1) это будет шаблон, основная имплементация основанная на наших предположениях. Более точно ответить на наши вопросы связанные с живым поведением оригинального вебсокет сервера я смогу когда он станет работать (через пару дней).

2) Мне нужен код прокси сервера, т.к. отправить соединение из браузера или из клаудфларе воркера я не смогу.
Думаю, что буду юзать бесплатный план на render.
Код буду держать в папке websocket (пока она пустая).
Я понимаю, что холодные старты могут занимать до минуты, но думаю что это не проблема -- как только ивент стартанет и первый пользователь загрузит мое приложение, то сервер уже будет жить коннекшены к нему от разных клиентов (в т.ч. реконнекты не дадут ему уснуть). А если будут дни когда нет схваток, то заснуть этому серверу и сэкономить выглядит как здравая идея.

Единственный момент, что мне хотелось бы содержать свой хттп прокси (для обхода корс) и вебсокет прокси (для обхода серверной защиты по ориджин) на одном фреймворке, но при этом для меня важно бесплатный план на постоянной основе с нормальными лимитами.
И для хттп серверя я доволен клаудфларе воркером, а вот для вебсокет сервера на сколько я понял на клаудфларе я бесплатно не смогу развернуться, поэтому мне подсказали рендер. Ну тоже неплохо, но вдруг ты предложишь что-то универсальное -- можем обсудить.

Статический сайт так и будет деплоится как гитхаб пейджес.

Также я думаю, что мой вебсокет прокси должен использовать модель обсервера, т.е. если 2 клинета запросили обновления по одному и тому же мат айди, то при получении такого события от оригинального вебсокет сервера мой вебсокет прокси должен прокинуть его всем клиентам которые на него подписались (разные браузерные табы на разных девайсах) -- имеет смысл?

3) вебсокет прокси будет в трех экземплярах (аналогично моему хттп прокси серверу): прод, дев и девтест(mode=test)

девтест будет присылать по кругу набор событий клиентам на которые они подписались (нужно будет составить файл со строками json типа как в server\dev-test-martialmatch-v1\data\websocket\new_connection.ws и сервер например каждую секунду будет отправлять клиентам следующую строку, эмулируя смену статуса, таймера, очков ... -- актуальный сценарий 628: server\dev-test-martialmatch-v1\data\628-x-superpuchar-polski-bjj-nogi-gi\628-wss-timeline.json , реген: node data/websocket/build-628-timeline.cjs )


4) в файле 
pl\events\current-matches\current-matches.js

можно будет подправить константы использующиеся для статуса боя с фактическими именами, которые использует мартиалматч из вебсокетов (это пока отложим, т.к. неясно как называется scheduled и есть ли он вообще) . Текущие текстовые названия на сколько я порнимаю -- это наша интерпретация и именно таких нет нигде в данных server\dev-test-martialmatch-v1\data, верно?

    if (!q || q.fightId !== fightId) return "scheduled";
    if (q.status === 2) return "active";
    if (q.status === 1) return "called";
    return "scheduled";



1) правильно ли я понимаю, что ты предлагаешь в итоге уйти от клаудфларе воркера к рендеру и уже на рендере иметь оба сервера: хттп прокси и вебсокет прокси? И что там лимитов и ресурсов мне хватит на оба сервера на фри плане (сейчас на бесплатном клаудфларе воркере мне хватает с многократным запаом, я думаю что не использую и 5 процентов лимитов..).
Есть ли какой-то недостаток переезда хттп сервера (фикс корс) с клаудфларе воркера на рендер (по железу, лимитам использования или стоимости -- я однозначно хочу все в фри план как на клаудфларе воркере сейчас)? 

2) Еще вопрос по тому, что наш вебсокет прокси будет делать если каждую минуту оригинальный рвет соединение?
Мы тоже будем рвать и форсить наших клиентов повторять процесс? Я не про оптимизацию (тем более микрооптимизацию), но про разумный выбор для нашего прокси -- как здесь логичнее поступить?

1) оставляем тогда клаудфларе воркер для хттп сервера как есть + разрабатываем вебсокет сервер на рендере (думаю использовать какой-то простой фреймворк для написания, т.к. задача выглядит примитивной, что скажешь?

2) а если не рвать соединения, то как отследить сабскрайберов, которые отвалились и почистить свой список обсервера, чтобы не отправлять им данные (или даже отписаться от ивентов с оригинального сервера если нет ни одно клиента заинтересованного в конкретном канале-мате)? или это как-то автоматически произойдет при ошибке во время отправки сообщения с нашего прокси такому клиенту (в смысле мы это сделаем явно в обработчике ошибки)? или какая логика?